name: 🚦 BPA Quality Check

on:
  push:
    paths:
      - '**/src/*.Report/**'
      - '**/src/*.SemanticModel/**'
    branches:
      - "feature/**"
      - "features/**"

  workflow_dispatch:

jobs:
  run-bpa-checks:                     # anciennement: bpa
    name: 🧪 Run Power BI Best Practices
    runs-on: windows-latest
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 📊 Semantic Model – BPA Analysis
        run: .\scripts\bpa-semanticmodel.ps1 -path "." -src "./src/*.SemanticModel"
      
      - name: 📈 Report – BPA Analysis
        run: .\scripts\bpa-report.ps1 -path "." -src "./src/*.Report"
